# 第一章 实训任务概述

## 1.1 任务背景

### 1.1.1 实训目的

计算机系统能力实训是计算机科学与技术专业教学计划中的重要实践教学环节，其目的是进一步加强学生对计算机组成原理、操作系统、编译原理、汇编语言等专业核心课程知识的理解和掌握。通过本次为期四周的综合性实训，学生需要将前期所学的理论知识进行综合运用，设计并实现一个计算机系统软件——高级编程语言编译器，从而培养计算机系统理论和技术的应用能力，提高学生的工程实践能力和创新意识。

编译器是计算机系统中最核心的系统软件之一，它承担着将高级编程语言编写的源程序翻译成计算机能够直接执行的机器代码的重要任务。编译器的设计与实现涉及形式语言与自动机理论、数据结构与算法、程序设计语言原理等多个计算机科学领域的知识，是计算机科学理论与实践相结合的典型应用。通过编译器的设计与实现，学生可以深入理解计算机系统的工作原理，掌握软件系统设计的基本方法，提高分析问题和解决问题的能力。

### 1.1.2 选题意义

本次实训选择"基于简单优先法的微型程序语言编译器设计与实现"作为题目，具有以下几方面的意义：

第一，简单优先法是一种经典的自底向上语法分析方法，它通过预先计算文法符号之间的优先关系来指导语法分析过程。与LL分析法和LR分析法相比，简单优先法的原理更加直观，实现相对简单，适合作为编译原理课程的实践教学内容。通过实现基于简单优先法的编译器，学生可以深入理解自底向上语法分析的基本原理和实现方法。

第二，微型程序语言（mico C）是一种简化的C语言子集，它保留了C语言的核心语法特性，包括变量声明、赋值语句、控制结构、函数定义等，同时简化了部分复杂的语法特性。通过设计和实现mico C语言的编译器，学生可以掌握程序语言设计的基本方法，理解编译器各个阶段的工作原理。

第三，本项目选择Hack虚拟机作为目标平台，使用VM指令集作为目标语言。Hack虚拟机是nand2tetris课程中的重要组成部分，它提供了一个简洁而完整的虚拟机架构。通过将编译器的目标代码在Hack虚拟机上运行，学生可以验证编译器的正确性，同时加深对虚拟机技术的理解。

### 1.1.3 实训要求

根据实训任务书的要求，编译器软件需按照软件工程的思想，完成系统软件的分析、设计及实现，翻译的目标代码能够在虚拟目标机上正确运行。具体而言，本次实训需要完成以下主要任务：

（1）源语言和目标语言的定义：采用符合编译器要求的文法定义一个结构完整的源语言程序，包含输入输出语句、赋值语句、循环结构的语句、选择结构的语句等，确定虚拟机及目标代码指令集。

（2）词法分析程序的设计及实现：根据源语言的词法规则，设计及实现源语言的词法分析程序，完成源程序单词的截取，识别词法错误，并转换成特征字序列，为语法分析做准备。

（3）语法分析程序设计及实现：选择简单优先语法分析方法，理解其主要思想，设计相关的分析表和语法分析步骤，完成源程序是否合法的判断，并指出其中的语法错误，具备容错功能。

（4）目标代码生成：设计语义翻译的属性文法，在语法分析的基础上，采用语法制导的方法实现目标代码的生成。

（5）目标代码的运行：利用已有虚拟机的解析器运行目标程序，得到与源程序完全一致的结果。通过多个测试用例验证编译器的正确性。

## 1.2 任务要求

### 1.2.1 软件工程要求

本次实训要求按照软件工程的思想进行编译器的开发，具体包括以下几个方面：

**需求分析阶段**：在项目开始之前，需要对编译器的功能需求和非功能需求进行详细分析。功能需求包括编译器需要支持的语言特性、输入输出格式、错误处理机制等；非功能需求包括性能要求、可维护性要求、可扩展性要求等。通过需求分析，明确编译器的边界和目标，为后续的设计和开发工作奠定基础。

**系统设计阶段**：在需求分析的基础上，进行编译器的总体设计和详细设计。总体设计确定编译器的整体架构、模块划分、接口定义等；详细设计对每个模块的算法、数据结构、实现细节进行详细说明。系统设计阶段需要输出设计文档，作为编码实现的依据。

**编码实现阶段**：按照设计文档进行编码实现，要求代码结构清晰、注释完善、符合编码规范。在编码过程中，需要注意模块之间的解耦，提高代码的可重用性和可维护性。

**测试验证阶段**：通过多个测试用例验证编译器的正确性。测试用例需要覆盖编译器的各个功能模块，包括正常情况和异常情况。测试过程中需要记录测试结果，分析和解决发现的问题。

### 1.2.2 功能要求

编译器需要支持以下语言特性，如表1-1所示：

**表1-1 编译器支持的语言特性**

| 类别 | 语言特性 | 说明 |
|------|---------|------|
| 数据类型 | int | 整数类型 |
| | char | 字符类型 |
| | double | 双精度浮点类型 |
| | 数组 | 一维数组声明和访问 |
| | 结构体 | 结构体定义和成员访问 |
| 变量操作 | 变量声明 | 声明变量并可选初始化 |
| | 赋值语句 | 变量赋值操作 |
| 算术运算 | 加法（+） | 两个操作数相加 |
| | 减法（-） | 两个操作数相减 |
| | 乘法（*） | 两个操作数相乘 |
| | 除法（/） | 两个操作数相除 |
| | 取模（%） | 求余数 |
| 比较运算 | 大于（>） | 大于比较 |
| | 小于（<） | 小于比较 |
| | 等于（==） | 等于比较 |
| | 不等于（!=） | 不等于比较 |
| | 大于等于（>=） | 大于等于比较 |
| | 小于等于（<=） | 小于等于比较 |
| 逻辑运算 | 逻辑与（&&） | 逻辑与运算 |
| | 逻辑或（\|\|） | 逻辑或运算 |
| | 逻辑非（!） | 逻辑非运算 |
| 控制结构 | if-else | 条件分支语句 |
| | while | 循环语句 |
| 输入输出 | read | 读取输入 |
| | write | 输出结果 |
| 函数 | 函数定义 | 定义函数 |
| | 函数调用 | 调用函数 |
| | 参数传递 | 值传递 |
| | return | 返回值 |

### 1.2.3 目标代码要求

编译器生成的目标代码需要满足以下要求：

第一，目标代码必须符合Hack虚拟机的VM指令集规范。Hack虚拟机是一种基于栈的虚拟机，其指令集包括栈操作指令（如push、pop）、算术逻辑指令（如add、sub、neg、eq、gt、lt、and、or、not）、流程控制指令（如label、goto、if-goto）、函数调用指令（如function、call、return）等。

第二，目标代码必须能够在Hack虚拟机上正确运行。编译器生成的VM代码经过VMTranslator转换为汇编代码，再经过Assembler转换为机器码，最终在CPUEmulator或VMEmulator上执行。整个工具链的每个环节都需要正确工作，才能保证最终结果的正确性。

第三，目标代码的执行结果必须与源程序的语义完全一致。编译器需要正确处理各种语言特性，包括变量作用域、函数调用约定、参数传递方式等，确保翻译的准确性。

### 1.2.4 文档要求

本次实训要求撰写完整的实训报告，报告内容包括：

（1）实训任务概述：介绍实训的背景、目的、要求和内容。

（2）源语言和目标语言：定义源语言的词法规则、语法规则等语法定义，以及目标语言的结构、语义等定义。

（3）编译器的需求分析：分析编译器的主要功能需求。

（4）编译器的设计：描述编译器的流程设计，包括词法分析、语法分析、目标代码生成等算法设计。

（5）编译器的实现和测试：展示编译器的实现细节和多个测试结果。

（6）实训小结：总结编译器的特点、实训体会和建议。

（7）参考文献：按规范格式要求撰写和引用。

## 1.3 任务内容

### 1.3.1 源语言和目标语言的定义

**源语言定义**

本项目定义的源语言称为mico C（微型C语言），它是一种简化的C语言子集。mico C语言保留了C语言的核心语法特性，包括：

（1）数据类型：支持int、char、double三种基本数据类型，以及数组和结构体两种复合数据类型。

（2）变量声明：支持变量的声明和初始化，可以在声明时指定变量的类型和初始值。

（3）赋值语句：支持将表达式的值赋给变量，包括简单赋值和复合赋值。

（4）算术运算：支持加、减、乘、除、取模五种基本算术运算。

（5）比较运算：支持大于、小于、等于、不等于、大于等于、小于等于六种比较运算。

（6）逻辑运算：支持逻辑与、逻辑或、逻辑非三种逻辑运算。

（7）控制结构：支持if-else条件分支语句和while循环语句。

（8）输入输出：支持read语句读取输入，write语句输出结果。

（9）函数：支持函数的定义、调用、参数传递和返回值。

mico C语言的文法采用BNF范式进行定义，详细内容将在第二章中介绍。

**目标语言定义**

本项目选择Hack虚拟机作为目标平台，使用VM指令集作为目标语言。Hack虚拟机是一种基于栈的虚拟机，其主要特点包括：

（1）基于栈的架构：Hack虚拟机使用栈来存储操作数和中间结果，大多数指令都是从栈中取操作数，将结果压入栈中。

（2）内存分段：Hack虚拟机将内存分为栈段（Stack）、堆段（Heap）、静态段（Static）、常量段（Constant）等不同的段，每段有不同的用途。

（3）函数调用机制：Hack虚拟机支持函数的定义和调用，使用栈帧来管理函数的局部变量和参数。

（4）指令集：Hack虚拟机的指令集包括以下几类指令：

- 栈操作指令：push segment index（将指定段的第index个元素压入栈）、pop segment index（将栈顶元素弹出到指定段的第index个位置）
- 算术逻辑指令：add（加法）、sub（减法）、neg（取反）、eq（等于比较）、gt（大于比较）、lt（小于比较）、and（逻辑与）、or（逻辑或）、not（逻辑非）
- 流程控制指令：label label（定义标签）、goto label（无条件跳转）、if-goto label（条件跳转）
- 函数调用指令：function functionName nLocals（定义函数）、call functionName nArgs（调用函数）、return（返回）

### 1.3.2 词法分析程序的设计及实现

词法分析是编译器的第一个阶段，其主要任务是将源程序的字符序列转换成Token序列。Token是词法分析的基本单位，每个Token包含两个信息：Token类型和Token值。

本次实训中，词法分析程序需要完成以下工作：

（1）单词截取：从源程序的字符序列中识别出一个个单词（Token）。单词的边界由分隔符（如空格、换行符、运算符等）确定。

（2）词法错误识别：在单词截取过程中，识别并报告词法错误。常见的词法错误包括：非法字符、数字格式错误、字符串未闭合等。

（3）Token序列生成：将识别出的单词转换成Token序列，每个Token包含类型和值两个属性。Token类型包括：关键字（KEYWORD）、标识符（IDENTIFIER）、数字（NUMBER）、字符（CHAR_LITERAL）、运算符（OPERATOR）、分隔符（DELIMITER）等。

（4）为语法分析做准备：将生成的Token序列传递给语法分析器，作为语法分析的输入。

词法分析器的设计采用有限自动机的理论，通过状态转换图来描述单词的识别过程。详细设计将在第三章中介绍。

### 1.3.3 语法分析程序设计及实现

语法分析是编译器的第二个阶段，其主要任务是根据语言的语法规则，分析Token序列的语法结构，判断源程序是否符合语法规则。

本次实训选择简单优先语法分析方法，这是一种经典的自底向上语法分析方法。简单优先法的基本思想是：

（1）为文法中的所有符号对预先计算优先关系（小于、等于、大于三种关系）。

（2）在语法分析过程中，根据栈顶符号和当前输入符号的优先关系，决定是进行移进操作还是归约操作。

（3）如果栈顶符号的优先级小于当前输入符号，则进行移进操作，将当前输入符号压入栈中。

（4）如果栈顶符号的优先级大于当前输入符号，则进行归约操作，将栈顶的某个子串归约为对应的非终结符。

（5）如果栈顶符号的优先级等于当前输入符号，则进行移进操作。

简单优先法的关键在于优先关系表的构建。优先关系表的构建需要计算FIRSTVT和LASTVT集合，然后根据产生式和FIRSTVT、LASTVT集合来确定符号对之间的优先关系。

本次实训中，语法分析程序需要完成以下工作：

（1）文法定义：定义mico C语言的文法，包括产生式、终结符、非终结符等。

（2）优先关系表构建：计算FIRSTVT和LASTVT集合，构建优先关系表。

（3）语法分析：使用简单优先法对Token序列进行语法分析，判断源程序是否符合语法规则。

（4）错误处理：在语法分析过程中，识别并报告语法错误，具备容错功能。

（5）语法树构建：在语法分析过程中，构建语法树（Parse Tree）和抽象语法树（AST），为后续的语义分析和代码生成提供基础。

详细设计将在第三章中介绍。

### 1.3.4 目标代码生成

目标代码生成是编译器的最后一个阶段，其主要任务是将语法分析和语义分析的结果转换成目标代码。

本次实训中，目标代码生成采用语法制导翻译的方法。语法制导翻译的基本思想是：

（1）在文法的产生式中嵌入语义动作（Semantic Action）。

（2）在语法分析过程中，当产生式被归约时，执行相应的语义动作。

（3）语义动作可以进行语义检查（如类型检查）、生成中间代码（如四元式）、生成目标代码等操作。

本次实训中，目标代码生成程序需要完成以下工作：

（1）语义检查：在语法分析过程中，进行语义检查，包括类型检查、变量声明检查、函数调用检查等。

（2）符号表管理：维护符号表，记录变量、函数等标识符的信息，包括类型、作用域、地址等。

（3）中间代码生成：生成四元式（Quadruple）作为中间代码。四元式是一种三地址码，每个四元式包含四个部分：运算符、操作数1、操作数2、结果。

（4）目标代码生成：将四元式转换成Hack虚拟机的VM指令。

详细设计将在第三章中介绍。

### 1.3.5 目标代码的运行

目标代码的运行是验证编译器正确性的重要环节。本次实训中，目标代码的运行流程如图1-1所示：

```
┌─────────────────────────────────────────────────────────────┐
│                    编译器工具链                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────┐      ┌─────────────┐      ┌─────────────┐│
│   │   源程序     │      │  编译器      │      │  VM代码     ││
│   │  (.txt)     │ ───→ │ ShiXvn_     │ ───→ │  (.vm)      ││
│   │             │      │ Compiler    │      │             ││
│   └─────────────┘      └─────────────┘      └─────────────┘│
│                                                    │        │
│                                                    ↓        │
│                                            ┌─────────────┐  │
│                                            │ VMTranslator │  │
│                                            └─────────────┘  │
│                                                    │        │
│                                                    ↓        │
│   ┌─────────────┐      ┌─────────────┐      ┌─────────────┐│
│   │  执行结果    │      │ CPUEmulator │      │  汇编代码   ││
│   │             │ ←─── │ /VMEmulator │ ←─── │  (.asm)     ││
│   │             │      │             │      │             ││
│   └─────────────┘      └─────────────┘      └─────────────┘│
│                                           ↑                 │
│                                           │                 │
│                                     ┌─────────────┐         │
│                                     │  Assembler  │         │
│                                     └─────────────┘         │
│                                           │                 │
│                                           ↓                 │
│                                     ┌─────────────┐         │
│                                     │  机器码     │         │
│                                     │  (.hack)    │         │
│                                     └─────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**图1-1 编译器工具链示意图**

**【图表说明】** 请绘制一个工具链流程图，展示从源代码到最终执行的完整流程。图中需要包含以下元素：
- 源程序文件（.txt）
- 编译器（ShiXvn_Compiler）
- VM代码文件（.vm）
- VMTranslator工具
- 汇编代码文件（.asm）
- Assembler工具
- 机器码文件（.hack）
- CPUEmulator或VMEmulator
- 执行结果
- 各个阶段之间用箭头连接，标注转换关系

工具链各阶段的说明如表1-2所示：

**表1-2 工具链各阶段说明**

| 阶段 | 输入 | 输出 | 工具 | 说明 |
|------|------|------|------|------|
| 编译 | 源程序（.txt） | VM代码（.vm） | ShiXvn_Compiler | 将源程序编译成VM指令 |
| 翻译 | VM代码（.vm） | 汇编代码（.asm） | VMTranslator | 将VM指令翻译成汇编代码 |
| 汇编 | 汇编代码（.asm） | 机器码（.hack） | Assembler | 将汇编代码汇编成机器码 |
| 执行 | 机器码（.hack） | 执行结果 | CPUEmulator/VMEmulator | 在虚拟机上执行机器码 |

### 1.3.6 编译流程

微型语言编译器的编译流程如图1-2所示：

```
┌─────────────────────────────────────────────────────────────┐
│                      编译流程                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    源程序                             │  │
│  │              int a = 10;                              │  │
│  │              int b = 20;                              │  │
│  │              int c = a + b;                           │  │
│  │              write(c);                                │  │
│  └───────────────────────┬──────────────────────────────┘  │
│                          ↓                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   词法分析                            │  │
│  │  输入：字符序列                                       │  │
│  │  输出：Token序列                                      │  │
│  │                                                      │  │
│  │  [KEYWORD:int] [ID:a] [OP:=] [NUM:10] [DELIM:;] ... │  │
│  └───────────────────────┬──────────────────────────────┘  │
│                          ↓                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   语法分析                            │  │
│  │  输入：Token序列                                      │  │
│  │  输出：语法树、抽象语法树                             │  │
│  │                                                      │  │
│  │  P → S ; S ; S ; S                                    │  │
│  │  S → int id = E                                       │  │
│  │  E → E + E | num | id                                 │  │
│  └───────────────────────┬──────────────────────────────┘  │
│                          ↓                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   语义分析                            │  │
│  │  输入：语法树                                         │  │
│  │  输出：四元式、符号表                                 │  │
│  │                                                      │  │
│  │  (=, 10, -, a)                                        │  │
│  │  (=, 20, -, b)                                        │  │
│  │  (+, a, b, t1)                                        │  │
│  │  (=, t1, -, c)                                        │  │
│  │  (write, c, -, -)                                     │  │
│  └───────────────────────┬──────────────────────────────┘  │
│                          ↓                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   代码生成                            │  │
│  │  输入：四元式                                         │  │
│  │  输出：VM指令                                         │  │
│  │                                                      │  │
│  │  push constant 10                                     │  │
│  │  pop local 0                                          │  │
│  │  push constant 20                                     │  │
│  │  pop local 1                                          │  │
│  │  push local 0                                         │  │
│  │  push local 1                                         │  │
│  │  add                                                 │  │
│  │  pop local 2                                          │  │
│  │  push local 2                                         │  │
│  │  call Output.printInt 1                               │  │
│  └───────────────────────┬──────────────────────────────┘  │
│                          ↓                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   目标程序                            │  │
│  │              VM代码（.vm文件）                        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**图1-2 编译器编译流程图**

**【图表说明】** 请绘制一个详细的编译流程图，展示编译器各个阶段的工作过程。图中需要包含以下元素：
- 源程序示例代码
- 词法分析阶段：输入（字符序列）、输出（Token序列）
- 语法分析阶段：输入（Token序列）、输出（语法树）
- 语义分析阶段：输入（语法树）、输出（四元式）
- 代码生成阶段：输入（四元式）、输出（VM指令）
- 目标程序（VM代码）
- 各阶段之间用箭头连接
- 可以使用不同颜色区分不同的阶段

编译流程各阶段的详细说明如表1-3所示：

**表1-3 编译流程各阶段说明**

| 阶段 | 主要任务 | 输入 | 输出 | 主要算法/技术 |
|------|---------|------|------|--------------|
| 词法分析 | 单词识别、Token生成 | 字符序列 | Token序列 | 有限自动机、正则表达式 |
| 语法分析 | 语法结构分析 | Token序列 | 语法树 | 简单优先法、移进-归约 |
| 语义分析 | 语义检查、中间代码生成 | 语法树 | 四元式 | 属性文法、语法制导翻译 |
| 代码生成 | 目标代码生成 | 四元式 | VM指令 | 指令选择、寄存器分配 |

## 1.4 开发环境

### 1.4.1 硬件环境

本次实训使用的硬件环境如表1-4所示：

**表1-4 硬件环境配置**

| 项目 | 配置 | 说明 |
|------|------|------|
| 处理器 | Intel Core i5-12400 | 6核12线程，主频2.5GHz |
| 内存 | 16GB DDR4 | 满足编译器运行需求 |
| 硬盘 | 512GB SSD | 提供足够的存储空间 |
| 显示器 | 1920×1080分辨率 | 提供良好的开发环境 |

### 1.4.2 软件环境

本次实训使用的软件环境如表1-5所示：

**表1-5 软件环境配置**

| 类别 | 软件 | 版本 | 说明 |
|------|------|------|------|
| 操作系统 | Windows | 11 | 开发和运行环境 |
| 编程语言 | C++ | 17 | 编译器实现语言 |
| 编译器 | GCC/G++ | 13.2.0 | C++编译器 |
| 构建工具 | CMake | 3.28 | 跨平台构建工具 |
| 开发工具 | Visual Studio Code | 1.85 | 代码编辑器 |
| 版本控制 | Git | 2.43 | 版本控制系统 |
| 调试工具 | GDB | 13.2 | 程序调试工具 |

### 1.4.3 目标平台

本次实训的目标平台如表1-6所示：

**表1-6 目标平台配置**

| 项目 | 说明 | 用途 |
|------|------|------|
| 目标虚拟机 | Hack虚拟机 | 运行目标代码 |
| 指令集 | VM指令集 | 目标代码格式 |
| 测试工具 | CPUEmulator | 硬件级模拟器 |
| 测试工具 | VMEmulator | 虚拟机级模拟器 |
| 汇编工具 | Assembler | 汇编代码转换 |

Hack虚拟机是nand2tetris课程中的重要组成部分，它提供了一个简洁而完整的虚拟机架构。Hack虚拟机的主要特点包括：

（1）基于栈的架构：使用栈来存储操作数和中间结果，指令从栈中取操作数，将结果压入栈中。

（2）内存分段：将内存分为栈段、堆段、静态段、常量段等不同的段，每段有不同的用途。

（3）函数调用机制：支持函数的定义和调用，使用栈帧来管理函数的局部变量和参数。

（4）指令集简洁：指令集包含约20条指令，涵盖了栈操作、算术逻辑、流程控制、函数调用等基本功能。

### 1.4.4 工具链

编译器工具链由多个工具组成，各工具之间的关系如图1-3所示：

```
┌─────────────────────────────────────────────────────────────┐
│                       工具链组成                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  工具1：ShiXvn_Compiler（编译器）                    │   │
│  │  功能：将源程序编译成VM代码                          │   │
│  │  输入：.txt文件                                      │   │
│  │  输出：.vm文件                                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  工具2：VMTranslator（VM翻译器）                     │   │
│  │  功能：将VM指令翻译成汇编代码                        │   │
│  │  输入：.vm文件                                       │   │
│  │  输出：.asm文件                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  工具3：Assembler（汇编器）                          │   │
│  │  功能：将汇编代码汇编成机器码                        │   │
│  │  输入：.asm文件                                      │   │
│  │  输出：.hack文件                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  工具4：CPUEmulator/VMEmulator（模拟器）             │   │
│  │  功能：执行机器码，得到运行结果                      │   │
│  │  输入：.hack文件                                     │   │
│  │  输出：执行结果                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**图1-3 工具链组成图**

**【图表说明】** 请绘制一个工具链组成图，展示各个工具之间的关系。图中需要包含以下元素：
- 四个工具：ShiXvn_Compiler、VMTranslator、Assembler、CPUEmulator/VMEmulator
- 每个工具的输入和输出文件格式
- 工具之间用箭头连接，表示数据流向
- 可以使用不同颜色区分不同的工具

各工具的详细说明如表1-7所示：

**表1-7 工具链各工具说明**

| 工具名称 | 功能 | 输入 | 输出 | 位置 |
|---------|------|------|------|------|
| ShiXvn_Compiler | 编译器 | .txt文件 | .vm文件 | D:\c_cpp\ShiXvn_Compiler\ |
| VMTranslator | VM翻译器 | .vm文件 | .asm文件 | D:\c_cpp\ShiXvn_Compiler\tools\ |
| Assembler | 汇编器 | .asm文件 | .hack文件 | D:\nand2tetris\tools\ |
| CPUEmulator | 硬件模拟器 | .hack文件 | 执行结果 | D:\nand2tetris\tools\ |
| VMEmulator | 虚拟机模拟器 | .vm文件 | 执行结果 | D:\nand2tetris\tools\ |

### 1.4.5 项目目录结构

本次实训的项目目录结构如表1-8所示：

**表1-8 项目目录结构**

| 目录/文件 | 说明 | 主要内容 |
|----------|------|---------|
| src/ | 源代码目录 | 编译器各模块的实现代码 |
| include/ | 头文件目录 | 编译器各模块的头文件 |
| output/ | 输出目录 | 编译器各阶段的输出文件 |
| output/Lexer/ | 词法分析输出 | tokens.txt |
| output/Parser/ | 语法分析输出 | parse_tree.txt、ast.txt |
| output/Codegen/ | 代码生成输出 | Main.vm、quads.txt |
| test/ | 测试用例目录 | 测试源程序文件 |
| tools/ | 工具目录 | VMTranslator等工具 |
| CMakeLists.txt | CMake配置文件 | 项目构建配置 |
| main.cpp | 主程序入口 | 编译器主函数 |

项目目录结构如图1-4所示：

```
ShiXvn_Compiler/
├── src/                        # 源代码目录
│   ├── Lexer.cpp              # 词法分析器实现
│   ├── Grammar.cpp            # 文法定义与处理
│   ├── PrecedenceParser.cpp   # 简单优先法语法分析器
│   ├── SemanticAnalyzer.cpp   # 语义分析器
│   ├── AST.cpp                # 抽象语法树
│   ├── CodegenVisitor.cpp     # 代码生成器
│   ├── SymbolTable.cpp        # 符号表管理
│   └── ErrorHandler.cpp       # 错误处理
├── include/                    # 头文件目录
│   ├── Lexer.h
│   ├── Grammar.h
│   ├── PrecedenceParser.h
│   ├── SemanticAnalyzer.h
│   ├── AST.h
│   ├── CodegenVisitor.h
│   ├── SymbolTable.h
│   └── ErrorHandler.h
├── output/                     # 输出目录
│   ├── Lexer/                 # 词法分析输出
│   │   └── tokens.txt
│   ├── Parser/                # 语法分析输出
│   │   ├── parse_tree.txt
│   │   ├── ast.txt
│   │   └── trace.txt
│   └── Codegen/               # 代码生成输出
│       ├── Main.vm
│       ├── Main.asm
│       ├── Main.hack
│       └── quads.txt
├── test/                       # 测试用例目录
│   ├── test_flow.txt          # 流程验证测试
│   └── test_bubble.txt        # 冒泡排序测试
├── tools/                      # 工具目录
│   └── VMTranslator.cpp       # VM翻译器
├── CMakeLists.txt              # CMake配置文件
├── main.cpp                    # 主程序入口
└── README.md                   # 项目说明文档
```

**图1-4 项目目录结构图**

**【图表说明】** 请绘制一个树形目录结构图，展示项目的完整目录结构。图中需要包含以下元素：
- 顶层目录：ShiXvn_Compiler
- 各个子目录和文件
- 使用缩进表示层级关系
- 可以使用不同图标区分目录和文件
- 在每个文件/目录旁边添加简要说明

## 1.5 本章小结

本章对基于简单优先法的微型程序语言编译器设计与实现的实训任务进行了全面的概述。

首先，在任务背景部分，介绍了本次实训的目的和意义，说明了编译器在计算机系统中的重要地位，以及选择简单优先法和mico C语言的原因。

其次，在任务要求部分，详细说明了软件工程要求、功能要求、目标代码要求和文档要求，明确了本次实训需要完成的具体任务。

然后，在任务内容部分，详细介绍了源语言和目标语言的定义、词法分析程序的设计及实现、语法分析程序设计及实现、目标代码生成、目标代码的运行等各个阶段的工作内容，并通过图表展示了编译流程和工具链。

最后，在开发环境部分，介绍了本次实训使用的硬件环境、软件环境、目标平台、工具链和项目目录结构，为后续章节的详细设计和实现奠定了基础。

通过本章的介绍，读者可以对本次实训任务有一个全面的了解，明确编译器需要实现的功能和达到的目标，为后续章节的学习提供指导。
